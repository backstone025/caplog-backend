package com.example.caplog.domain.ai.capture;

import org.springframework.stereotype.Service;

@Service
public class captureAiService {
    String parseCaptureToText(String captureToText){
        return captureToText;
    }
}
